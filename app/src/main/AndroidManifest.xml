<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shss.restaurantwaiter"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="26" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".volleynetwork.ControllerRequest"
        android:allowBackup="true"
        android:icon="@mipmap/rohlogot_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.LoginActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.TableActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.CategoryActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.ProductActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.ComplementsProductActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.CartActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.PaymentActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.AboutActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.Printersetting"/>

        <activity android:name=".activity.DeviceListActivity"/>
    </application>

</manifest>